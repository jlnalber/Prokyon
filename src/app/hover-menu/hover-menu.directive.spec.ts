import { HoverMenuDirective } from './hover-menu.directive';

describe('HoverMenuDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new HoverMenuDirective();
    expect(directive).toBeTruthy();
  });
});
